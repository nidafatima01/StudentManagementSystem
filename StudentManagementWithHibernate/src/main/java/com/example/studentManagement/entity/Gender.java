package com.example.studentManagement.entity;

//Each constant in the enum represents a possible gender option to ensure type safety when working with gender-related data in the application
public enum Gender {
    MALE,
    FEMALE,
    OTHER
}
